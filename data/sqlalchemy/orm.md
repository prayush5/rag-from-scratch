# SQLAlchemy ORM Overview

The Object-Relational Mapper (ORM) maps Python classes to database tables, transforming database rows into standard Python objects.

## Key Features

- **Declarative Base**: The base class that every mapped table model inherits from.
- **Type Safety**: Maps database types (like integer, string, boolean) directly to Python types.
