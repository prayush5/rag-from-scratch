# Python Decorators

A decorator is a function that takes another function as an argument, extends its behavior, and returns a new modified function without altering the original code.

## Popular Applications

- Logging function calls and execution duration.
- Authentication and authorization checks in web applications.
- Caching expensive function return values.
